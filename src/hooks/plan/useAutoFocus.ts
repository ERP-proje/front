// import { useEffect, useRef } from "react";

// const useAutoFocus = <T extends HTMLElement>(shouldFocus: boolean) => {
//   const ref = useRef<T | null>(null);

//   useEffect(() => {
//     if (shouldFocus && ref.current) {
//       ref.current.focus();
//     }
//   }, [shouldFocus]);

//   return ref;
// };

// export default useAutoFocus;
